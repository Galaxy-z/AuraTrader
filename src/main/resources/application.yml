spring:
  application:
    name: AuraTrader

binance:
  testnet: false
  api:
    key: ${BINANCE_API_KEY}
    secret: ${BINANCE_API_SECRET}
  pairs:
    - ETHUSDT
    - BTCUSDT
    - BNBUSDT
    - XRPUSDT

proxy:
  host: ${PROXY_HOST}
  port: ${PROXY_PORT}
